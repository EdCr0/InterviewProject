import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mainButtonContainer: {
        backgroundColor: '#0527FB',
        borderRadius: 10,
        padding: 8,
        alignItems:'center'
    },
    buttonTitle:{
        color: '#FFF',
        fontWeight: "bold",
        fontSize: 18
    },
});